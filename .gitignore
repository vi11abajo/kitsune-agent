node_modules/
dist/
*.log
.DS_Store
.npmrc

# Local-only dev memory & internal docs — must NOT reach this PUBLIC repo
# (protocol/infra notes live in the separate, private protocol repo)
CLAUDE.md
docs/
